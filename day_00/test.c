#include<stdio.h>
int main() {
	int i ;
	int j ;
	char arr1[] = "abc";
	char arr2[] = {'a','b','c'};
	int sz1 = sizeof(arr1) / sizeof(arr1[0]);
	int sz2 = sizeof(arr2) / sizeof(arr2[0]);
	return 0;
}